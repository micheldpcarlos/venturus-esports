import { WeekDaysTextPipe } from './week-days-text.pipe';

describe('WeekDaysTextPipe', () => {
  it('create an instance', () => {
    const pipe = new WeekDaysTextPipe();
    expect(pipe).toBeTruthy();
  });
});
